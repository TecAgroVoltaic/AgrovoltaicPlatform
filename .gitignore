# Secretos / entorno
.env
/env
.venv

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Acumulado de uso/costo de los agentes (estado local, no se versiona)
.uso/

# Artefactos del pipeline
/output/
/data/
*.parquet

# Dumps de base de datos (grandes + datos de produccion ajenos, NO commitear)
/sql/dump/
*.dump
*.dump.gz

# macOS junk
.DS_Store

# Tooling local (config de maquina, no se versiona)
.claude/
.mcp.json
skills-lock.json

# Datos crudos: NO se versionan en git (viven en el .zip / storage / la DB de Supabase)
dataset/Monitoreo-AgroVoltaic-SC-NEW/
dataset/*.zip
dataset/*.db
