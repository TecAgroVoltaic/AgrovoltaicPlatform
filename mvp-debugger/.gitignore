node_modules
.next
out
.env.local
# Los respaldos con fecha del .env.local tienen los MISMOS secretos que el
# original. Sin esta linea, un `git add -A` los publica.
.env.local.bak*
.env.local.*
*.log
.DS_Store

# build info de TypeScript
*.tsbuildinfo

# Arbol compilado del arnes de verificacion (scripts/verificar-vistas.mjs)
.verify
.vercel
.env*

# Credenciales de produccion en texto plano. NUNCA se versionan.
CREDENCIALES-*.txt
